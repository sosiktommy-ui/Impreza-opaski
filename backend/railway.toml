[deploy]
startCommand = "npx prisma generate && npx ts-node prisma/pre-migrate.ts && npx prisma db push --accept-data-loss && npx ts-node prisma/seed.ts && node dist/main.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
