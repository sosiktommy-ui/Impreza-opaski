[deploy]
startCommand = "npx prisma generate && npx prisma db push && npx ts-node prisma/seed.ts && node dist/main.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
